import React, { Component } from 'react';
import '../App.css';

export default class Viewer extends Component{
    render(){
        return(
            <div className="viewer">
                <h1>Viewer</h1>
            </div>
        );
    }
}
