import React, { Component } from 'react';
import '../App.css';

export default class Manifest extends Component{
    render(){
        return(
            <div>
                <h1>Manifest</h1>
            </div>
        );
    }
}
